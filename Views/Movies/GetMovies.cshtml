@* <div style="padding-top:30px">
    <h3>Список фильмов</h3>
    <table class="table">
        <tr>
            <td><p>Название</p></td>
            <td><p>Год</p></td>
            <td><p>Жанр</p></td>
            <td><p>Режиссер</p></td>
            <td><p>Описание</p></td>
            </tr>
        @foreach (var movie in ViewBag.Movies)
        {
            <tr>
                <td><p>@movie.Name</p></td>
                <td><p>@movie.Year</p></td>
                <td><p>@movie.Genre</p></td>
                <td><p>@movie.Director</p></td> 
                <td><img src="@movie.Poster" alt="Постер фильма @movie.Name"></td>
                 <td><p>@movie.Description</p></td> 
                </tr>
        }
    </table>
</div> *@


@model IEnumerable<hw.Models.Movie>

<div style="padding-top:30px">
    <h3>Список фильмов</h3>
    <p>
    <a asp-action="Create">Create New</a>
</p>
  

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(movie => movie.Name)
            </th>
            <th>
                @Html.DisplayNameFor(movie => movie.Year)
            </th>
            <th>
                @Html.DisplayNameFor(movie => movie.Genre)
            </th>
            <th>
                @Html.DisplayNameFor(movie => movie.Director)
            </th>
            <th>
                @Html.DisplayNameFor(movie => movie.Poster)
            </th>
             <th>
                @Html.DisplayNameFor(movie => movie.Description)
            </th>
        </tr>
    </thead>
    <tbody>

@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Director)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Poster)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
